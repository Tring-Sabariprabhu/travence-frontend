import CustomDialog from "../../../Components/CustomDialog/CustomDialog"

export const ExpenseRemainder = ()=>{
    return (
        // <CustomDialog open={false} onClose={function (): void {
        //     throw new Error("Function not implemented.")
        // } } dialog_title={""} children={undefined}>

        // </CustomDialog>
        <p></p>
    )
}